/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'financedesk.xyz',
            'api.financedesk.xyz',
        ],
    },
}

export default nextConfig
